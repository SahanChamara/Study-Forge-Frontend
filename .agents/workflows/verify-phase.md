# /verify-phase <number>

Verify the requested frontend phase without adding unrelated features.

Check acceptance criteria, TypeScript, lint, tests, production build, responsive behavior, accessibility basics, route protection, mock mode, and API adapter boundaries. Record failures in `.agents/state/PHASE_STATUS.md`.
