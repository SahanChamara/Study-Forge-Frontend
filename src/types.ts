export type TopicStatus='not_started'|'learning'|'practicing'|'review'|'mastered';export interface LearningPath{id:string;title:string;description:string;goal:string;targetLevel:string;updatedAt:string;modules?:ModuleItem[];topics?:Topic[]}export interface ModuleItem{id:string;title:string;description:string;order:number}export interface Topic{id:string;moduleId:string;title:string;objective:string;estimatedMinutes:number;order:number;status:TopicStatus;mastery:number;resourceUrls:string[]}export interface Note{id:string;pathId:string;topicId:string;title:string;contentMarkdown:string;tags:string[];updatedAt:string}export interface PracticeTask{id:string;pathId:string;topicId:string;title:string;instructions:string;type:string;status:'todo'|'doing'|'done';evidence:string}
