# /feature <frontend feature>

Implement one frontend feature only.

- Read product and API contract docs first.
- Keep components independent from transport details.
- Add/update mock fixtures before UI implementation where data is required.
- Cover loading, empty, error, and success states.
- Add appropriate tests.
- Do not create backend code.
