---
name: frontend-react
description: Implement React/TypeScript UI features for StudyForge.
---

# React Frontend Skill
Work in `apps/web`. Keep API calls centralized. Firebase client is for Authentication only in MVP. Every async page needs loading/error/empty states. Prefer simple semantic UI. Never render untrusted note Markdown as raw HTML without sanitization. Build before handoff.
