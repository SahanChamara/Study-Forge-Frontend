# Keep mock mode while designing the frontend independently.
VITE_DATA_MODE=mock
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Firebase Web SDK values are only needed when real Firebase Auth is enabled.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
